# MailSender Skill

Sends emails using the MailerSend API.

## Usage

```bash
node send.js --to "recipient@example.com" --subject "Subject Line" --text "Body text"
```

## Configuration

Requires valid API key in `send.js`.
